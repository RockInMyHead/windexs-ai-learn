# 🚨 Исправление ошибки OpenAI API: "Country, region, or territory not supported"

## Проблема
Сервер выдает ошибку: `unsupported_country_region_territory` при запросах к OpenAI API.

## Решение

### 1. Автоматическая обработка ошибок
Добавлена функция `safeOpenAICall()`, которая:
- Автоматически обнаруживает geo-blocking ошибки
- Переключается на прямое подключение к OpenAI (без прокси)
- Предоставляет fallback ответы при недоступности API

### 2. Исправления в коде
- ✅ Обновлены дефолтные настройки прокси на актуальные
- ✅ Добавлена обработка ошибок для всех вызовов OpenAI API
- ✅ Добавлен fallback на прямое подключение при проблемах с прокси
- ✅ Исправлена модель gpt-5.1 на gpt-4o (gpt-5.1 не существует)

### 3. Развертывание исправленной версии

```bash
# На сервере обновите код
cd /var/www/teacher.windexs.ru
git pull origin main

# Перезапустите сервер
pm2 restart teacher-ai

# Проверьте логи
pm2 logs teacher-ai --lines 50
```

### 4. Тестирование

```bash
# Тест health check
curl https://teacher.windexs.ru/api/health

# Тест чата (должен работать даже при проблемах с прокси)
curl -X POST https://teacher.windexs.ru/api/chat/general \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"content":"Привет","messageType":"text"}'
```

### 5. Мониторинг

В логах сервера теперь будут сообщения:
- `🌍 Geo-blocking detected, attempting proxy bypass...` - при обнаружении блокировки
- `🔄 Switching to direct OpenAI connection...` - при переключении на прямое подключение
- `✅ Direct connection works!` - при успешном fallback

### 6. Переменные окружения

Убедитесь, что на сервере установлены:
```bash
OPENAI_API_KEY=your_key_here
PROXY_ENABLED=true
PROXY_HOST=185.68.186.158
PROXY_PORT=8000
PROXY_USERNAME=7BwWCS
PROXY_PASSWORD=BBBvb6
```

## Результат
Теперь при проблемах с прокси система автоматически переключается на прямое подключение к OpenAI, обеспечивая стабильную работу приложения.
